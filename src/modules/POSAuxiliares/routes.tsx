import type { RouteObject } from 'react-router';

// Todas las páginas han sido migradas a sus módulos propios.
// Este archivo se mantiene vacío para no romper imports existentes.
// Las rutas activas están en:
//   Configuracion/routes.tsx  — ajustes, config-pos, email, cloudinary, mercadopago
//   Clientes/routes.tsx       — cuenta-corriente (ver clientesRoutes)
//   Productos/routes.tsx      — listas-precio (ver productosRoutes)
//   Ventas/routes.tsx         — ventas, cotizaciones, facturacion, despachos, reportes, auditoria

export const posAuxiliaresRoutes: RouteObject[] = [];
